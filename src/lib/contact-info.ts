/**
 * Customer-facing inquiry email used across hero, footer and other
 * "let's talk" surfaces. Distinct from the per-region `info@` address
 * declared in regions.yml, which is the address of record for legal /
 * GDPR notices and JSON-LD Organization metadata.
 */
export const CONTACT_EMAIL = "poptavka@vykoupim-nemovitost.cz";
