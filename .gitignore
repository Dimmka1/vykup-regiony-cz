node_modules/
.next/
.vercel

# Env: ignore every variant (incl. `vercel env pull` output), keep only the template
.env
.env.*
!.env.example

# Playwright
test-results/
playwright-report/
blob-report/
audit-copy.txt
.lighthouseci/
